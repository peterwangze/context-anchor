{
  "name": "context-anchor",
  "version": "0.2.0",
  "private": true,
  "description": "Session memory persistence, validation, and skillification runtime for OpenClaw",
  "scripts": {
    "doctor": "node scripts/doctor.js",
    "configure:host": "node scripts/configure-host.js",
    "configure:sessions": "node scripts/configure-sessions.js",
    "diagnose:sessions": "node scripts/sessions-diagnose.js",
    "status:sessions": "node scripts/sessions-status.js",
    "install:host": "node scripts/install-one-click.js",
    "test": "node --test tests/*.test.js"
  }
}
